name: consumer_app
description: A simple consumer app demonstrating core_api_client_package usage.
publish_to: 'none'

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  core_api_client_package:
    path: ..

flutter:
  uses-material-design: true
